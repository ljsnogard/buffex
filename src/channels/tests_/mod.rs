//! `channels` 模块的测试。
//!
//! * [`spsc_`]——SPSC 队列：构建、FIFO 往返、有界背压、关闭 / EOF、两端迭代接口；
//! * [`mpsc_`]——MPSC 队列：构建期参数校验、共享缓冲的多写者同步写入、读端迭代
//!   接口、以及**异步写路径当前显式不支持**这一骨架期契约；
//! * [`conc_segm_`]——channels 专用的并发写段：一格一条消息、乱序上报、全部归还
//!   后由 `Drop` 提交；要更多就再从父段切一段。



mod conc_segm_;
mod mpsc_;
mod spsc_;
